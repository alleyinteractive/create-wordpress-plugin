name: Built Release

on:
  push:
    branches:
      - develop
      - main
      - production

jobs:
  built-asset:
    uses: alleyinteractive/.github/.github/workflows/built-release.yml@main
    with:
      node: 16

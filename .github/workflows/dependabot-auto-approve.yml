# This workflow is disabled by default. To enable it, uncomment the lines below.
# name: dependabot-auto-approve
# on:
#   pull_request:
#
# permissions:
#   pull-requests: write
#   contents: write
#
# jobs:
#   dependabot:
#     uses: alleyinteractive/.github/.github/workflows/dependabot-auto-approve.yml@main

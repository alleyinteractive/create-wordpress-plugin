<?php
/**
 * ${{ name | psrClassName }} test class file
 *
 * @package create-wordpress-plugin
 */

namespace Create_WordPress_Plugin\${{ inputs.featureName | wpNamespace('Tests\\Features') }};

use Create_WordPress_Plugin\Tests\TestCase;
use Create_WordPress_Plugin\${{ inputs.featureName | wpNamespace('Features') }}\${{ inputs.featureName | wpClassName }};

/**
 * ${{ inputs.featureName | psrClassName | replace('_', ' ') }} Test for the ${{ inputs.featureName | wpClassName }} class.
 *
 * @link https://mantle.alley.com/docs/testing
 */
class ${{ inputs.featureName | psrClassName }} extends TestCase {
	/**
	 * An example test for the feature.
	 */
	public function test_example() {
		$this->assertTrue( true );
	}
}
